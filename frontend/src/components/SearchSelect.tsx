import { Check, ChevronDown, Search } from "lucide-react";
import { useEffect, useRef, useState } from "react";
export type PickerOption = { value: string; label: string; detail?: string };
export function SearchSelect({ value, onChange, options, placeholder = "Select an option", searchable = true, className = "" }: { value: string; onChange: (value: string) => void; options: PickerOption[]; placeholder?: string; searchable?: boolean; className?: string }) {
  const [open, setOpen] = useState(false); const [query, setQuery] = useState(""); const root = useRef<HTMLDivElement>(null);
  useEffect(() => { const close = (event: MouseEvent) => { if (!root.current?.contains(event.target as Node)) setOpen(false); }; document.addEventListener("mousedown", close); return () => document.removeEventListener("mousedown", close); }, []);
  const selected = options.find((option) => option.value === value); const filtered = options.filter((option) => option.label.toLowerCase().includes(query.toLowerCase()));
  return <div ref={root} className={`relative ${className}`}><button type="button" aria-haspopup="listbox" aria-expanded={open} onClick={() => setOpen(!open)} className="flex w-full items-center justify-between rounded-xl border border-white/15 bg-white/5 px-4 py-3 text-left text-sm text-white hover:border-[#d6ff57]/60"><span className={selected ? "text-white" : "text-white/45"}>{selected?.label || placeholder}</span><ChevronDown className={`h-4 w-4 text-[#d6ff57] transition-transform ${open ? "rotate-180" : ""}`} /></button>{open && <div className="absolute z-50 mt-2 max-h-64 w-full overflow-hidden rounded-xl border border-white/15 bg-[#1b1b18] p-2 shadow-2xl">{searchable && <label className="mb-2 flex items-center gap-2 rounded-lg bg-white/5 px-3 py-2"><Search className="h-4 w-4 text-white/45" /><input autoFocus value={query} onChange={(event) => setQuery(event.target.value)} className="min-w-0 flex-1 bg-transparent text-sm text-white outline-none" placeholder="Search…" /></label>}<div className="max-h-48 overflow-y-auto scrollbar-hide" role="listbox">{filtered.map((option) => <button key={option.value} type="button" role="option" aria-selected={value === option.value} onClick={() => { onChange(option.value); setOpen(false); setQuery(""); }} className="flex w-full items-center justify-between rounded-lg px-3 py-2.5 text-left text-sm text-white/75 hover:bg-white/10 hover:text-white"><span><span className="block">{option.label}</span>{option.detail && <span className="block text-xs text-white/40">{option.detail}</span>}</span>{value === option.value && <Check className="h-4 w-4 text-[#d6ff57]" />}</button>)}{filtered.length === 0 && <p className="px-3 py-4 text-sm text-white/45">No matches found.</p>}</div></div>}</div>;
}